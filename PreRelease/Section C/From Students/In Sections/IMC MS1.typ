#import "../../../../Templates/ms-template.typ": *
#mark-scheme[
  == IMC One
#cs-ms-table(
    questions:(
      (
        number: 1,
        marks: 1,
        solution: [
          `TypeOfAnt`;
        ]
      ),
      (
        number: 2,
        marks: 1,
        solution: [
          To reduce/change the stored quantity of food in the nest; \
          To prevent the quantity of food from becoming negative;

          *Max 1*
        ]
      ),
      (
        number: 3,
        marks: 1,
        solution: [
          String;
        ]
      ),
      (
        number: 4,
        marks: 2,
        solution: [
          #align(center)[
          #table(columns:(150pt,80pt),
            table.cell(fill:gray.lighten(50%))[Component],table.cell(fill:gray.lighten(50%))[Order],
            table.cell(fill:gray.lighten(50%))[Nests],text(red)[2],
            table.cell(fill:gray.lighten(50%))[Food in Cells],text(red)[4],
            table.cell(fill:gray.lighten(50%))[Ants],text(red)[3],
            table.cell(fill:gray.lighten(50%))[Cells],text(red)[1]
          )]
        ]
      ),
      (
        number: 5,
        marks: 4,
        solution: [
          Within the method `Main`, before the first selection structure; \
          Ask the user for an input within an (indefinite) iterative structure; \
          Compare the input with the list of valid inputs;\
          If the input is valid, exit the iterative structure;\
          Continue iterating until a valid input is provided;

          *Max 4*
        ]
      )
    )
  )
]
